// import createCache from "@emotion/cache";
// import { EmotionCache } from "@emotion/react";

// /**
//  * Creates an Emotion cache instance for managing styles.
//  *
//  * This function initializes an Emotion cache instance with the specified configuration options.
//  * The cache is used to store and manage styles, improving performance by reusing stylesheets.
//  *
//  * @returns {EmotionCache} - The Emotion cache instance.
//  */
// export default function createEmotionCache(): EmotionCache {
//   // Create an Emotion cache with the specified options.
//   // - `key` is used to namespace styles in the cache.
//   // - `prepend` ensures that new styles are inserted at the beginning of the stylesheet,
//   //   improving the cascade order of styles.
//   return createCache({ key: "mui-styles", prepend: true });
// }
