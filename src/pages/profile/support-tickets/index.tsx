import RootLayout from "@/pages/components/layout";
import ProfileLayout from "../components/layout";

type Props = {};
export default function SupportTicketPage({}: Props) {
  return (
    <RootLayout>
      <ProfileLayout>SupportTicketPage</ProfileLayout>
    </RootLayout>
  );
}
